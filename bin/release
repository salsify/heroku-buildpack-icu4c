#!/usr/bin/env bash
# bin/release <build-dir>

cat << EOF
---
config_vars:
  PATH: vendor/icu4c/bin:bin:/usr/local/bin:/usr/bin:/bin
  CFLAGS: vendor/icu4c/include
  LIBRARY_PATH: vendor/icu4c/lib
  ICU_DIR: vendor/icu4c
EOF
